# Cheap complete DM read via MCP (card #735) — Spec

**Goal:** Give every MCP host three DM tools so a cell can take a
**cheap complete** unread batch (full bodies, typed fields, one call),
reply without a content-file dance, and fetch a thread — without
importing a cursor or a separate ack.

This is a spec. It does not change the live MCP server.

Pairs with #729 (why a cell does not SEE a DM). This card is what a
DM **costs** once awake. Not the delivery fix (#263).

## Measured (lab-ovh, 40-message window)

| form | size | ~tok | property |
|---|---|---|---|
| `mesh inbox --peek --json` | 70,604 B | ~17,651 | complete; must parse |
| `mesh inbox --peek` | 8,272 B | ~2,068 | affordable; **bodies truncated** (~160 chars/line) |
| content alone | 54,160 B | ~13,540 | irreducible meaning |
| envelope | 16,444 B | ~4,111 | **23%** of the complete read |

There is no cheap complete CLI read today. The price signal selects
`--peek` prose; that already cost 77 minutes on cursor-lin status
DM id=32049 (1,483 chars read as a 78-char preview).

MCP today: six tools (`search`, `add`, `describe`, `codegraph_query`,
`memory_navigate`, `timeline_navigate`). **DM tools: zero.** Every
read is bash + `--json` + a retyped parser (billed again).

## Mechanism: three tools, batch-shaped

Wire plain helpers in `mcp_server.py` (same pattern as
`_timeline_navigate`) and expose them on FastMCP. Identity /
gateway / token resolve the same way other mesh verbs do
(`--as` / `$SWARPH_SELF` / peer token file) — never ambient-consume
another peer's unread without an explicit self (#578 family).

### 1. `swarph_dm_unread()`

- Returns **all unread** DMs for the resolved self in **one** call.
- Each item: typed fields at minimum `id`, `from_node`, `to_node`,
  `kind`, `created_at`, `thread_id`, **`content` (FULL string)**.
- **No paging. No cursor. No next/previous/close.**
- Default: **peek semantics** (do not mark read). Consuming read is
  NOT the tool's job — see "ack" below.
- Envelope: prefer a **lean JSON schema** (drop null noise keys the
  gateway echoes) so the call is measurably cheaper than an equivalent
  `--json` window of the same messages. Content bytes stay intact.
- Cap: if unread count exceeds a hard safety ceiling (propose **100**),
  return the oldest N full bodies **plus** a loud top-level field
  `truncated_count` / `refused_silent_truncation: false` naming how many
  remain. Never silently drop. Never shorten a body to fit.

### 2. `swarph_dm_reply(id, text, kind="answer")`

- One call. Body is the `text` argument — **no content-file**.
- Routes by message id (existing `mesh reply` behaviour).
- Does **not** close board obligations (`board obligations close` stays
  the close act — #562).

### 3. `swarph_dm_thread(id)`

- Returns the conversation for that message / thread_id, full bodies.
- Same lean schema as unread.

### Hard exclusions (commander-confirmed)

| forbidden | why |
|---|---|
| `next` / `previous` / `close` | imports a **cursor** — hidden cross-turn state; LLM-worst; makes the 21:21-drain / 21:22-arrival miss systematic |
| separate `ack` verb | another forgettable act (assignment-without-dispatch shape). **Replying is the ack.** Anything still unacked keeps appearing in `dm_unread()`. |

Do not mark-read on `dm_unread()`. If a cell needs consume-on-read for
a PullSink ledger, that remains the CLI `mesh inbox` path (or a later
card). This card removes the incentive to read less; it does not
redefine monitor ACK.

## Summary: index vs filter, and how to pick a model

Commander asked whether a semantic AI summary could replace `--peek`
for long DMs. Answer lands on #585: the secretary is an **INDEX**, not
a filter.

| surface | policy |
|---|---|
| **Directed DMs via `dm_unread`** | **full bodies always** — a model summary must never *replace* the tool payload |
| **Optional index / channel bulk** | may summarise **only** if chosen by `swarph bench`, not by preference |

Lab 07:0xZ: commander shaped the can-fail as a **bench pack**. That
makes the summariser a **measured** choice (`swarph bench` already
caught one wrong model when price pointed the other way).

### Pack structure (`swarph bench validate` / `run`)

```
scenario   = a real DM (verbatim — never synthetic)
steps      = layered context + requirements (what the reading cell knows / owes)
injection  = IF message has thread_id, step PULLS thread text so the
             summary is scored on FOLLOW-THROUGH, not isolation
expected   = target output (payload-retention shaped — below)
```

The injection clause is load-bearing: a one-message summariser cannot
know that "acked 32200" carries an obligation forward. Continuity is
what failed last night; the pack must test it.

### Scorer = payload retention, NOT summary quality

Binary / deterministic. Optimising "good prose" drops ids and
reproduces the polished dismissal that cost 77 minutes.

| check | rule |
|---|---|
| RETAINED? | every card id, obligation id, PR number, thread_id |
| RETAINED? | the ASK (question / blocker / request) |
| RETAINED? | any REFUSAL or correction |
| COMPRESSED? | ratio vs source — returning the input unchanged **FAILS** |

Winner = high retention + high compression. Unchanged input fails
compression; aggressive compression fails retention.

### Fixture sizes — do NOT use 17.6k as a DM length

**17.6k tok is the complete inbox envelope dump** (this card's subject),
**not** any single DM. Lab correction 32591 measured lab-ovh inbox
n=4769: median ~304 tok, p95 ~1,060, max ~8.2k. Fixture 32049 is
1,484 chars ≈ **371 tok** (near median), `thread_id` NULL.

Interesting inversion band ≈ **300–4,000 tok**, not 17k. Pack must
span that range or it measures nothing about real DMs.

Local small models are **not** auto-disqualified on window alone at
true DM sizes (a 4k window covers ~p95). Window ≠ summary quality —
do not conflate ([[feedback_narrowing_is_not_validating]]).

`num_ctx` is still mandatory: truncation risk enters mainly through
**thread injection**, which can push a ~300-tok message to several
thousand. Default ollama 2048 will bite after injection.

### Three fixtures (lab 32591 — span range + injection)

| id | from | chars | thread_id | role |
|---|---|---|---|---|
| **32049** | cursor-lin | 1,484 | NULL | specified; near median; **no** injection |
| **30510** | drop-on-meta-edge | 12,481 | 81570cab | long + thread → injection |
| **24081** | gemini-researcher | 16,908 | 44941bf5 | longest threaded — **REPLACE**: only 2 card ids, zero msg-ids; no resolving power (lab 32609) |

Expected shape for 32049 (unchanged): group by what it demands of the
**reader** (owed / asks / refused / notice / blocked); ids verbatim;
refusal on its own line; "BLOCKED, no action" kept. ~10 lines vs 1,483
chars. >>> "cursor-lin progress update" = REJECT. <<<

Optional fourth: short ack ("acked 32124, holding") — assert no pad.

### Scorer (from 32518 + 32591)

Automated / deterministic: extract card ids (`#\d{2,4}`), PR numbers,
message ids (`3\d{4}`), thread UUIDs from SOURCE; count verbatim hits
in summary → want / retained / missed / compression ratio.

**Range notation:** expand `#51-#58` (and similar) before scoring, or
score range-hits separately — naive regex drops the interior and
punishes better compression (lab 32609 / claude).

**Hand gates (not automated):** ASK retained, REFUSAL retained — no
honest keyword proxy ([[feedback_a_paraphrase_cannot_prove_absence]]).
Read saved outputs. Retention number alone ≠ all three gates.

### Context-length warning

Droplet 2026-07-31 inversion still applies at the **300–4k** band.
Snippet benches predict nothing. Arms are **lane-labelled** where
served model id is unknown (gpt/claude placeholders) — do not read the
table as a model ranking; only qwen2.5:7b-instruct has a knowable model
id among current $0 arms.

## Bench lane wiring (lab audit 07:13–07:15Z) — IN SCOPE for build

Measured on installed 0.54.0: `backends.py` ships three classes —
metered Gemini, metered Mistral, and `SubscriptionBackend` stub
(`call_fn` callers=0). **No ollama class.** Every $0 transport the
fleet owns is outside the dispatch dict. Commander hard-gates new
spend (metered keys unset on lab-ovh).

### $0 lanes — measure `/delegate`, never `/health` alone

Lab can-fail 07:2xZ (PONG prompt). Health-200 lied twice:

| lane | /health | POST /delegate | verdict |
|---|---|---|---|
| claude-service :8787 | 200 | 200, ~5.5s | **WORKS** |
| gpt-service :8789 | 200 | 200, PONG | **WORKS** |
| gemini-service :8790 | 200 | 503 OIDC absent | **BROKEN** (agy warm + refresh) |
| grok-service :8791 | 200 | 500 missing binary in bwrap | **BROKEN** (path allowlist) |

Working $0 subscription lanes **today: 2**, not 4. Gemini/grok repairs are
**prerequisites**, not pack content. `/health` = listener bound; never
lane can serve. (grok even reports `grok_authed: true` with `grok_cli: null`.)

Local generation: gpu-wsl ollama `http://100.84.11.104:11434` (5 tags).
lab-ovh localhost ollama is embed-only. True DM sizes (median~304 /
p95~1k / max~8k tok) do **not** auto-disqualify small local tags on
window alone; prefer **qwen2.5:7b-instruct** as the quality floor and
treat smaller tags as optional experiments — never conflate a window
fail with a quality fail.

### Build adds (both in `swarph_cli/bench/backends.py`)

1. **Wire `SubscriptionBackend.call_fn`** to services layer:
   `POST http://100.64.189.91:{port}/delegate` with Bearer, return text.
   Docstring already left this seam to the caller (decision #3).
2. **Add a local ollama backend.** Surface is **UNRESOLVED** until
   verified on gpu-wsl: amendment 32540 said `/v1/chat/completions`, but
   that compat layer historically may **not** pass `options.num_ctx`
   (context comes from Modelfile). If still true, use `/api/chat` with
   explicit `options.num_ctx`, or Modelfile overrides — **resolve before
   locking the surface**; do not ship `/v1` as written without the
   can-fail below. Base URL from **env** (gpu-wsl tailnet as DATA —
   [[feedback_a_remedy_has_an_expiry_date]]).

**Metered Mistral stays spend-gated** (no `base_url` seam). Local
Mistral open-weights = ollama tag via the local backend, iff VRAM.

**`num_ctx` mandatory + recorded** on every local call (or proven
Modelfile equivalent). Default 2048 truncates silently.

**CAN-FAIL:**
- dead subscription port → per-arm failure **reported**, not silent skip
- missing local tag → same
- local surface: assert the request's effective context window is the
  one the pack set (not 2048 default) — if `/v1` cannot do that, it
  fails this can-fail and `/api/chat` wins

**VRAM:** ask gpu-wsl before 7B / mistral load
([[feedback_gpu_wsl_vram_coordination]]).

### Pack report requirements (lab 32544)

**(a) Arms are lane-labelled, not model-labelled.** A gemini-service
arm can be 2.5-flash one call and 3-flash-preview the next. Pack MUST
**record the served model per call** when the lane reports one; when it
does not, print `lane-level arm, served model unknown` — never put the
lane name where a model id belongs. Unknown is a refusal to name, not a
name. (gpt `/delegate` returned placeholder `<subscription-default>`.)

**(b) Claude preamble.** claude-service PONG reported
`cache_creation_input_tokens≈62868` for a two-token prompt — ~63k
subscription preamble. Record it; **do not silently average** latency /
tokens with other arms.

### Recommended pack roster ($0, no new spend)

**Runnable now:** `gpt-service`, `claude-service` (ceiling; note
preamble).

**Conditional (skip with reason until fixed):** `gemini-service`,
`grok-service`.

**Local (after gpu-wsl OK + num_ctx surface resolved):**
`qwen2.5:7b-instruct` floor; local mistral tag iff VRAM.

Not shortlisted: DeepSeek, Cursor cell models, **metered** Mistral API.

### Bench baseline (lab-ovh, 2026-09-06 07:50–07:58Z) — measured

3 arms × 3 fixtures, 9/9 ok, $0. Payload retention (ids in SOURCE that
appear in SUMMARY):

| arm | 32049 (no inject) | 30510 (inject) | 24081 | latency |
|---|---|---|---|---|
| gpt-service | 18/18 | **10/14** | 2/2 | 12.9–17.3s |
| claude-service | 18/18 | **7/14** | 1/2 | 81–160s |
| qwen2.5:7b (`num_ctx=16384`) | 14/18 | **3/14** | 1/2 | 38.9–62.2s |

**Only 30510 discriminates** (order gpt > claude > qwen). Do not treat
32049/24081 columns as evidence without reading the caveats:

1. **24081** — replace with an identifier-**dense** long DM (want=2 is a
   coin flip; length ≠ density).
2. **32049** — compression degenerate (~0.82–0.85) when the prompt's
   word cap exceeds source length; not a model fail.
3. **Range notation** — scorer regex treats `PRs #51-#58` as only #51
   and #58. Biases against economical summaries. Build must expand
   ranges before scoring **or** record range-hits separately.
4. gpt/claude arms are **lane-labelled** (served model unknown); only
   qwen's model id is knowable — not a model ranking.

Hand gates on 30510: ASK retained on gpt (explicit "Reader action:") and
claude (structural Q-headings); both kept refusals/corrections. qwen
3/14 = polished-dismissal failure mode this card exists to prevent.

Latency under-reported: claude 5–10× slower than gpt for worse retention
on the discriminating fixture + ~63k preamble. For inbox batch, that is
drain-finishes vs does-not.

**Provisional recommendation (weak — one discriminating fixture):**
`gpt-service` default arm. qwen fine for median DMs (14/18 on 32049),
**not** drop-in for identifier-dense long ones. Before deciding, add ≥2
more identifier-dense long fixtures — one discriminating row is an
anecdote with a table.

Local `num_ctx=16384` **did** run on this pass — surface choice that
achieved it must be recorded in the build (resolves part of the /v1 vs
`/api/chat` open question: whatever lab used, lock that + can-fail it).

### Ship rule

- Cut may ship **MCP DM tools first** with summariser + lane wiring
  **as follow-on commits on the same card** (or clearly linked) — do
  not claim the summariser measured until lanes + pack are runnable.
- Do **not** ship a production summariser until `swarph bench validate`
  + `run` are green on this pack against the **production**
  prompt/context (no stand-in —
  [[feedback_simulated_stimulus_is_not_the_event]]).

## Accept

1. `swarph_dm_unread()` fixture: full bodies byte-equal; no truncation.
2. Cost measurably less than equivalent `--json` window (lean envelope).
3. Truncating bodies to save tokens = rebuilt defect.
4. No next/prev/close/ack in tool schema.
5. README / guide mention the three DM tools once.
6. Summariser either absent or selected by bench pack (fixtures
   32049 / 30510 / 24081, retention scorer + hand ASK/REFUSAL gates,
   real DM lengths not the 17.6k envelope) on the $0 roster above.
7. Subscription + Ollama backends: can-fails green (dead port / missing
   tag report per-model, never silent skip).

## Out of scope

| out | owner |
|---|---|
| wake / delivery / sinks=pull | #263, #729 |
| CLI `--peek` truncation redesign | may follow |
| board obligation close via reply | #562 |
| dreaming enrich crash exit codes | #734 |
| new metered API spend | commander gate |

## Related

- #585 secretary is an INDEX
- #587 inbox cannot report truncation
- #263 pull is a mailbox
- #729 dead wake path
- #718 carry / follow-through
- #101 / `swarph bench` pack runner
- [[feedback_verbosity_is_a_fleet_cost]]
